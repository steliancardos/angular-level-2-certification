import { ActivateRouteDirective } from './activate-route.directive';

describe('ActivateRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivateRouteDirective();
    expect(directive).toBeTruthy();
  });
});
